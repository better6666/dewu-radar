# 中国 5 大电商平台「商品搜索 + 价格」接口实测报告

> 调研目标：寻找可被 `curl/requests` 直接调用、尽量无需登录或复杂签名的接口，用于「按货号搜索商品并获取价格」。
>
> 实测环境：AWS 数据中心出口 IP（13.231.39.218，ap-northeast-1）；UA `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36`。
>
> 结论先行：**5 个平台中，只有苏宁易购可以在数据中心 IP 下无登录、无签名自动抓取价格。** 其余 4 家（京东/淘宝/拼多多/唯品会）的搜索与价格接口均被签名或设备指纹风控拦截，curl 直连不可行。

---

## 1. 京东（JD）—— 评级 C（价格接口另评 D）

### 搜索接口
| 项 | 内容 |
|---|---|
| PC 搜索 | `GET https://search.jd.com/Search?keyword=xxx&enc=utf-8` → HTTP 200，但返回的是 React SPA 空壳（`create-react-app`），**无任何商品数据服务端渲染**（页面 `searchMainConfig.isList = 0`） |
| 真实数据接口 | `GET https://api.m.jd.com/api?appid=search-pc-java&functionId=pc_search_searchWare&body={...}` |
| 移动搜索 | `GET https://so.m.jd.com/ware/search.action?keyword=xxx` → 302 跳转到风控验证页（`cfe.m.jd.com/privatedomain/risk_handler`，标题「京东验证」） |

实测命令与结果：
```bash
curl -s -L -A "$UA" "https://search.jd.com/Search?keyword=DD1503-101&enc=utf-8"
# → 200，React 壳 HTML，无商品

curl -s -A "$UA" "https://api.m.jd.com/api?appid=search-pc-java&functionId=pc_search_searchWare&body=%7B%22keyword%22%3A%22DD1503-101%22%7D"
# → HTTP 403  {"code":"1","echo":"cross-origin request from '' is not allowed"}
```

**关键困难**：从搜索页 JS（`preload-search.js`）反解出，`api.m.jd.com/api` 强制要求：
- `h5st` 签名（由 `window.PSign.sign()` 生成，基于 SHA256 + 动态 token，即京东的 h5st 签名体系）
- `x-api-eid-token`（由 `window.getJsToken()` 生成的风控 token）
- 页面还加载了 `js_security` / `pc-core-disposal.js` 风控脚本。

### 价格接口
| 项 | 内容 |
|---|---|
| 价格接口 | `GET https://p.3.cn/prices/mgets?skuIds=J_<skuId>` → **数据中心 IP 直接 SSL 握手失败**（`SSL_ERROR_SYSCALL`），无法连接 |
| 备用域名 | `https://pm.3.cn/...`、`http://p.3.cn/...` 同样失败 |
| 商品页 | `https://item.jd.com/<skuId>.html` → 200 但价格是 JS 骨架（`price-ske`），无服务端价格 |

实测：
```bash
curl -s -A "$UA" "https://p.3.cn/prices/mgets?skuIds=J_100012043978"
# → exit 35: SSL_ERROR_SYSCALL（连接被重置）
```

### 能否拿销量
搜索接口本身需签名，无法到达数据层；**拿不到**。

### 结论
**不可行。** 京东 PC 搜索需 h5st 签名 + jsToken 风控（评级 C），且价格 CDN `p.3.cn` 在本数据中心 IP 下被墙（评级 D）。除非逆向 h5st 或换住宅 IP + 无头浏览器，curl 直连无法自动抓价。

---

## 2. 淘宝（Taobao）—— 评级 C

### 搜索接口
| 项 | 内容 |
|---|---|
| PC 搜索 | `GET https://s.taobao.com/search?q=xxx` → 200，返回 SPA 壳，页面含 `initBaxia`/`Slide`（阿里 Baxia 滑动风控），数据由 XHR 加载 |
| 移动搜索 | `GET https://s.m.taobao.com/search?q=xxx` → **404**（旧移动搜索已下线） |
| mtop 搜索 API | `GET https://h5api.m.taobao.com/h5/mtop.taobao.wsearch.appsearch/1.0/?...&data={"q":"xxx"}` |

实测（无 token/sign）：
```bash
curl -s -A "$UA" "https://h5api.m.taobao.com/h5/mtop.taobao.wsearch.appsearch/1.0/?jsv=2.6.1&appKey=12574478&t=1700000000000&sign=dummy&api=mtop.taobao.wsearch.appsearch&v=1.0&data=%7B%22q%22%3A%22DD1503-101%22%7D"
# → {"ret":["RGV587_ERROR::SM::哎哟喂,被挤爆啦,请稍后重试!"],"data":{"url":"https://login.taobao.com/member/login.jhtml..."}}
```

**关键困难**：
- mtop 搜索接口要求登录态 `_m_h5_tk` cookie（未登录直接返回登录跳转 URL）；
- 需要 `sign`（mtop 签名，依赖登录 token 计算）；
- 需要过 Baxia 滑动验证码风控。

### 价格接口
无登录无法获取商品 ID；价格也走 mtop（需同套登录 + 签名）。

### 能否拿销量
未登录拿不到搜索数据，销量字段不可达。

### 结论
**不可行。** 淘宝搜索必须登录（未登录被 RGV587 拦截）+ mtop 签名 + Baxia 风控（评级 C）。curl 直连无法获取搜索结果与价格。

---

## 3. 拼多多（PDD）—— 评级 C

### 搜索接口
| 项 | 内容 |
|---|---|
| 移动搜索 | `GET https://mobile.yangkeduo.com/search_result.html?search_key=xxx` → 200，返回 Next.js 壳，**无 `__NEXT_DATA__` 服务端数据**，列表由 API 动态加载 |
| 真实数据接口 | `GET https://mobile.yangkeduo.com/proxy/api/search?search_key=xxx&page=1` |
| 备用 API | `GET https://api.pinduoduo.com/api/search?search_key=xxx&page=1` |

实测：
```bash
curl -s -A "$UA" -e "https://mobile.yangkeduo.com/" \
  "https://mobile.yangkeduo.com/proxy/api/search?search_key=DD1503-101&page=1&pdduid=0"
# → HTTP 403  {"server_time":...,"error_code":40001,"empty_reason":1}

curl -s -A "$UA" "https://api.pinduoduo.com/api/search?search_key=DD1503-101&page=1"
# → HTTP 403  {"error_msg":"","error_code":40003}
```

**关键困难**：拼多多搜索强制要求 `anti_content` 签名（`error_code 40001` 即缺 anti_content）。该签名由混淆 JS 在浏览器运行时生成，无法用简单脚本复现。

### 价格接口
同需 anti_content；商品详情价格同样走加密接口。

### 能否拿销量
被 anti_content 拦截，拿不到。

### 结论
**不可行。** 拼多多需逆向 `anti_content` 签名（评级 C），curl/requests 直连无法抓取。

---

## 4. 唯品会（Vipshop）—— 评级 C

### 搜索接口
| 项 | 内容 |
|---|---|
| PC 搜索页 | `GET https://category.vip.com/suggest.php?keyword=xxx` → 200，但只返回页面壳（artTemplate 模板），商品列表 JS 动态加载 |
| 真实数据接口 | `GET https://mapi-pc.vip.com/vips-mobile/rest/shopping/pc/search/product/rank`（JSONP，回调 `getMerchandiseIds`） |
| 参数 | `standby_id=nature`、`keyword`、`sort=0`、`pageOffset=0`、`channelId=1`、`gPlatform=PC`、`batchSize=120`、`api_key=<固定公开值>` |

实测：
```bash
curl -s -A "$UA" -e "https://category.vip.com/" \
 "https://mapi-pc.vip.com/vips-mobile/rest/shopping/pc/search/product/rank?standby_id=nature&keyword=DD1503-101&sort=0&pageOffset=0&channelId=1&gPlatform=PC&batchSize=120&api_key=70f71280d5d547b2a7bb370a529aeea1&callback=getMerchandiseIds"
# → {"msg":"Forbidden by TFS","code":11000}
```
（`api_key=70f71280d5d547b2a7bb370a529aeea1` 为页面 JS 中硬编码的公开值，非秘密；但即便带上仍被 TFS 拦截。）

**关键困难**：唯品会 mapi 接口除 `api_key` 外，还要求 **TFS（腾讯天御）设备指纹 `tfs_fp_token`**（由 TFS SDK 通过 canvas/WebGL 等生成），并可能触发 TFS 验证码。带假 token 或走 suggest 联想接口同样返回 `Forbidden by TFS`。

### 价格接口
价格随搜索列表接口（`product/rank` 返回的 `data.products[]` 内含价格）一并返回，但同样被 TFS 拦截。

### 能否拿销量
被 TFS 拦截，拿不到（接口正常时 `product/rank` 返回 `total` 与 `products[]`）。

### 结论
**不可行。** 唯品会需 TFS 设备指纹 `tfs_fp_token`（评级 C），curl 直连无法生成，需无头浏览器执行其 JS 才有机会。

---

## 5. 苏宁易购（Suning）—— 评级 A ✅

**唯一一个可在数据中心 IP 下无登录、无签名、纯 curl 完成「搜索 + 价格」的平台。**

### 5.1 搜索接口
| 项 | 内容 |
|---|---|
| URL | `GET https://search.suning.com/emall/searchV1Product.do?keyword=<关键词>&pg=01` |
| 方法 | GET |
| 请求头 | 仅需常见 UA（实测无需 Referer/Cookie） |
| 返回 | **服务端渲染的 HTML 片段**，每条商品是一个 `<li class="item-wrap">` |
| 每页条数 | 约 30 条，翻页改 `pg=01/02/03...` |

实测命令与返回：
```bash
curl -s -A "$UA" -e "https://search.suning.com/" \
  "https://search.suning.com/emall/searchV1Product.do?keyword=iPad&pg=01"
# → HTTP 200，HTML；numFound=218

# 单条商品结构（关键字段）：
# <li docType="1" id="0010328655-12451427546" hasPrice="false">
#   标题:  <div class="title-selling-point"><a href="//product.suning.com/0010328655/12451427546.html">联想生态品牌异能者平板电脑X12A ...</a></div>
#   价格:  <span class="def-price" datasku="12451427546||ssl|||0010328655||" brand_id="000051303" mdmGroupId="R1704002"></span>   ← 空，价格需二次请求
#   评价:  <i>0</i>评价
# </li>
```

**可从搜索 HTML 直接拿到的字段**：商品标题 ✅、商品码/货号 ✅（`id="<供应商码>-<商品码>"`）、商品链接 ✅（`product.suning.com/<供应商码>/<商品码>.html`）、评价数 ✅。
**拿不到**：价格（`hasPrice="false"`，需二次请求）、销量（苏宁搜索结果不含销量字段）。

### 5.2 价格接口（无签名，已验证）
| 项 | 内容 |
|---|---|
| URL | `GET https://ds.suning.com/ds/generalForTile/{item}-{cityId}-{type}-{supplierCode}-1--{callback}.json` |
| 方法 | GET（JSON 直出；加 `callback=xxx` 后缀或 `.jsonp` 即 JSONP） |
| 请求头 | 仅需 UA，**实测无需 Referer / Cookie** |

`{item}` 由搜索 HTML 的 `datasku` + `brand_id` + `mdmGroupId` 拼接：
```
partnumber18 = 商品码补零到 18 位（datasku.split("|")[0]，如 "12451427546" → "000000012451427546"）
type         = datasku.split("|")[2]           （如 "ssl"，自营；第三方可能为空）
supplierCode = datasku.split("|")[5]           （如 "0010328655"）
threegroupId = mdmGroupId                      （如 "R1704002"）
brandId      = brand_id                        （如 "000051303"）
item         = partnumber18 + "_____" + threegroupId + "_" + brandId + "__"
```

实测命令与返回（**真实价格**）：
```bash
curl -s -A "$UA" \
 "https://ds.suning.com/ds/generalForTile/000000012451427546_____R1704002_000051303__-025-ssl-0010328655-1--ds0000000001234.json"
# → {"status":200,"rs":[{"cmmdtyCode":"000000012451427546","price":"1399.00","priceType":"1","bizCode":"0010328655","type":"ssl","vendorName":"","invStatus":"1", ...}]}

curl -s -A "$UA" \
 "https://ds.suning.com/ds/generalForTile/000000000520502703_____R9013274_000283H02__-025--0000000000-1--ds0000000001234.json"
# → {"status":200,"rs":[{"cmmdtyCode":"000000000520502703","price":"149.00",...,"vendorName":"苏宁自营",...}]}
```

**价格字段在返回结构中的位置**：`rs[0].price`（字符串，元，含两位小数）；另有 `vendorName`（店名）、`invStatus`（有货状态 1/3）、`priceType` 等。多商品可把多个 `{item}` 用逗号拼接一次请求返回 `rs[]` 数组。

### 5.3 能否拿销量
**不能。** 苏宁搜索结果 HTML 与价格接口均不含销量字段，只有「评价数」（`<i>N</i>评价`）。

### 5.4 结论
**可行 ✅。** 苏宁是 5 个平台中唯一能实现「数据中心 IP + 无登录 + 无签名 + curl/requests」端到端抓取价格的目标：
1. `search.suning.com/emall/searchV1Product.do` 按关键词/货号搜索 → 拿标题、商品码、链接、`datasku`/`brand_id`/`mdmGroupId`、评价数；
2. `ds.suning.com/ds/generalForTile/...` 拿价格。

注意：货号含连字符（如 `DD1503-101`）时苏宁会退化为模糊匹配（拆成 "101"），且苏宁不一定有该货号在售；普通关键词（如 "iPad"）精确可用。

---

## 汇总对比

| 平台 | 搜索接口能否 curl 直连 | 价格能否单独查 | 可行性评级 | 能否拿销量 | 数据中心 IP 自动抓价结论 |
|---|---|---|---|---|---|
| 京东 | ❌ 需 h5st+jsToken | ❌ p.3.cn 被墙 + 需签名 | C（价格 D） | ❌ | **不可行** |
| 淘宝 | ❌ 需登录+mtop sign+Baxia | ❌ 同 mtop | C | ❌ | **不可行** |
| 拼多多 | ❌ 需 anti_content | ❌ 同 anti_content | C | ❌ | **不可行** |
| 唯品会 | ❌ 需 TFS 设备指纹 | ❌ 同 TFS | C | ❌ | **不可行** |
| 苏宁易购 | ✅ 服务端渲染 HTML | ✅ 无签名 JSON | **A** | ❌（仅评价数） | **可行** |

## 工程建议
- **立即可用**：苏宁易购（唯一 A 级）。按货号/关键词搜索 → 解析 `datasku`/`brand_id`/`mdmGroupId` → 调 `ds.suning.com` 拿价格。
- 若业务必须覆盖京东/淘宝/拼多多/唯品会，则需要：住宅代理 IP + 无头浏览器（Playwright/Selenium 执行各平台 JS 以生成 h5st / anti_content / TFS 指纹 / 登录态），本质是「浏览器自动化」而非「HTTP 接口调用」，且需维护反爬对抗。
